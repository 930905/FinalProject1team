<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="org.zerock.mapper.NoticeBoardMapper">

	<sql id="criteria">
			<trim prefix="(" suffix=") AND " prefixOverrides="OR">
			<foreach item="type" collection="typeArr">
				<trim prefix="OR">
					<choose>
						<when test="type == 'T'.toString()">
							title like '%'||#{keyword}||'%'
						</when>
						<when test="type == 'C'.toString()">
							content like '%'||#{keyword}||'%'
						</when>
						<when test="type == 'W'.toString()">
							writer like '%'||#{keyword}||'%'
						</when>
					</choose>
				</trim>
			</foreach>		
		</trim>	
	</sql>



	<select id="getList" resultType="org.zerock.domain.NoticeBoardVO">

	<![CDATA[
		SELECT * from notice WHERE bno > 0
	]]>	
	</select>
	

	<select id="read" resultType="org.zerock.domain.NoticeBoardVO">
		SELECT * from notice where bno = #{bno}
	</select>
	 
	
	<select id="getListWithPaging" resultType="org.zerock.domain.NoticeBoardVO">
		<![CDATA[
		SELECT BNO, TITLE, CONTENT, WRITER, REGDATE, MODDATE
		FROM (
			SELECT
			/* + INDEX_DESC(notice notice_pk) */
			ROWNUM rn, bno, title, content, WRITER, REGDATE, MODDATE
			from notice
			WHERE
		]]>
		
	
		<include refid="criteria"></include>
	
			
		<![CDATA[
			 ROWNUM <= #{pageNum} * #{amount}
			)
		WHERE RN > (#{pageNum} - 1) * #{amount}
		]]>	
	</select>
	
	
 	<select id="getTotalCount" resultType="int">
		SELECT COUNT(*) from notice
		WHERE
		<include refid="criteria"></include>
		
		bno > 0
	</select>    

</mapper>
